<h2>New Device</h2>

<%= render "form.html", changeset: @changeset,
                        action: device_path(@conn, :create),
                        users: @users %>

<span><%= link "Back", to: device_path(@conn, :index), class: "btn btn-default" %></span>
